// step / next / break <line> / breakpoints / remove <line> / continue
// watch <var> / unwatch <var> / watches / inspect

Func add(a, b) {
    var sum = a + b;
    return sum;
}

Func square_sum(a, b) {
    var s = add(a, b);
    return s * s;
}

var result = square_sum(3, 4);
print result;
